---
interface Props {
	classNameWrapper: string;
}

const { classNameWrapper } = Astro.props;
---


<p class={`js-color-theme ${classNameWrapper} cursor-pointer pointer-events-auto`}>
    color == '<span data-name="colorName">white</span>'
</p>